import request from '@/utils/request.js'

export const {{ apiName }} = (params: any):Promise<object> => {
    return request({
        method: '{{ method }}',
        url: '{{ baseUrl }}',
        params,
    })
}